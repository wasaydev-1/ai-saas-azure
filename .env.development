# Browser calls same-origin /api/v1 (Next proxy). Do not point at the backend IP here
# or the browser will hit CORS. Optional: http://localhost:3000/api/v1
# NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Server-side proxy target (Spring, etc.)
# BACKEND_API_URL=http://163.61.91.156:8080

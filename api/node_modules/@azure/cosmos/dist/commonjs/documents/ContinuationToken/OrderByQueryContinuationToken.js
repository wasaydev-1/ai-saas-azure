var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var OrderByQueryContinuationToken_exports = {};
__export(OrderByQueryContinuationToken_exports, {
  createOrderByQueryContinuationToken: () => createOrderByQueryContinuationToken,
  parseOrderByQueryContinuationToken: () => parseOrderByQueryContinuationToken,
  serializeOrderByQueryContinuationToken: () => serializeOrderByQueryContinuationToken
});
module.exports = __toCommonJS(OrderByQueryContinuationToken_exports);
function createOrderByQueryContinuationToken(rangeMappings, orderByItems, rid, skipCount, documentRid, offset, limit, hashedLastResult) {
  if (!rangeMappings || rangeMappings.length === 0) {
    throw new Error("rangeMappings must contain at least one element");
  }
  if (!orderByItems || orderByItems.length === 0) {
    throw new Error("orderByItems must contain at least one element");
  }
  return {
    rangeMappings,
    orderByItems,
    rid,
    skipCount,
    documentRid,
    offset,
    limit,
    hashedLastResult
  };
}
function serializeOrderByQueryContinuationToken(token) {
  return JSON.stringify(token);
}
function parseOrderByQueryContinuationToken(tokenString) {
  return JSON.parse(tokenString);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createOrderByQueryContinuationToken,
  parseOrderByQueryContinuationToken,
  serializeOrderByQueryContinuationToken
});

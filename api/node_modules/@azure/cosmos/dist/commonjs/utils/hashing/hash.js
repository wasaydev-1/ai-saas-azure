var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var hash_exports = {};
__export(hash_exports, {
  binarySearchOnPartitionKeyRanges: () => binarySearchOnPartitionKeyRanges,
  hashPartitionKey: () => hashPartitionKey
});
module.exports = __toCommonJS(hash_exports);
var import_documents = require("../../documents/index.js");
var import_batch = require("../batch.js");
var import_multiHash = require("./multiHash.js");
var import_v1 = require("./v1.js");
var import_v2 = require("./v2.js");
function hashPartitionKey(partitionKey, partitionDefinition) {
  const kind = partitionDefinition?.kind || import_documents.PartitionKeyKind.Hash;
  const isV2 = partitionDefinition && partitionDefinition.version && partitionDefinition.version === import_documents.PartitionKeyDefinitionVersion.V2;
  switch (kind) {
    case import_documents.PartitionKeyKind.Hash:
      return isV2 ? (0, import_v2.hashV2PartitionKey)(partitionKey) : (0, import_v1.hashV1PartitionKey)(partitionKey);
    case import_documents.PartitionKeyKind.MultiHash:
      return (0, import_multiHash.hashMultiHashPartitionKey)(partitionKey);
  }
}
function binarySearchOnPartitionKeyRanges(partitionKeyRanges, hashedPartitionKey) {
  let low = 0;
  let high = partitionKeyRanges.length - 1;
  while (low <= high) {
    const mid = Math.floor((low + high) / 2);
    const range = partitionKeyRanges[mid];
    if ((0, import_batch.isKeyInRange)(range.minInclusive, range.maxExclusive, hashedPartitionKey)) {
      return range.id;
    } else if (hashedPartitionKey.localeCompare(range.minInclusive) < 0) {
      high = mid - 1;
    } else {
      low = mid + 1;
    }
  }
  return void 0;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  binarySearchOnPartitionKeyRanges,
  hashPartitionKey
});

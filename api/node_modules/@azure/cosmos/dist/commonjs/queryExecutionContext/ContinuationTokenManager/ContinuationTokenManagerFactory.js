var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ContinuationTokenManagerFactory_exports = {};
__export(ContinuationTokenManagerFactory_exports, {
  ContinuationTokenManagerFactory: () => ContinuationTokenManagerFactory
});
module.exports = __toCommonJS(ContinuationTokenManagerFactory_exports);
var import_ParallelQueryContinuationTokenManager = require("./ParallelQueryContinuationTokenManager.js");
var import_OrderByQueryContinuationTokenManager = require("./OrderByQueryContinuationTokenManager.js");
const ContinuationTokenManagerFactory = {
  /**
   * Creates the appropriate continuation token manager based on query type.
   * Only call this for queries that support continuation tokens.
   * @param collectionLink - Collection link/RID
   * @param initialContinuationToken - Optional initial continuation token
   * @param isOrderByQuery - Whether this is an ORDER BY query
   * @returns Appropriate continuation token manager instance
   * @internal
   */
  create(collectionLink, initialContinuationToken, isOrderByQuery = false) {
    if (isOrderByQuery) {
      return new import_OrderByQueryContinuationTokenManager.OrderByQueryContinuationTokenManager(collectionLink, initialContinuationToken);
    } else {
      return new import_ParallelQueryContinuationTokenManager.ParallelQueryContinuationTokenManager(collectionLink, initialContinuationToken);
    }
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ContinuationTokenManagerFactory
});

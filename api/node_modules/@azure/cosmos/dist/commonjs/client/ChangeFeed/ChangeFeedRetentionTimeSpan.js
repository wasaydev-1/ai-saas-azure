var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ChangeFeedRetentionTimeSpan_exports = {};
__export(ChangeFeedRetentionTimeSpan_exports, {
  ChangeFeedRetentionTimeSpan: () => ChangeFeedRetentionTimeSpan
});
module.exports = __toCommonJS(ChangeFeedRetentionTimeSpan_exports);
var import_request = require("../../request/index.js");
class ChangeFeedRetentionTimeSpan {
  retentionInMinutes;
  /**
   * @internal
   */
  constructor(minutes) {
    if (typeof minutes !== "number") {
      throw new import_request.ErrorResponse("ChangeFeedRetentionTimeSpan must be a number.");
    }
    if (minutes < 0) {
      throw new import_request.ErrorResponse("ChangeFeedRetentionTimeSpan must be a positive number.");
    }
    if (minutes % 1 !== 0) {
      throw new import_request.ErrorResponse("Retention's granularity is minutes.");
    }
    this.retentionInMinutes = minutes;
  }
  /**
   * Specifies the retention window in minutes for which processing the change feed with allVersionsAndDeletes mode will be available.
   */
  static fromMinutes(minutes) {
    return new ChangeFeedRetentionTimeSpan(minutes);
  }
  /**
   * @internal
   */
  getRetentionInMinutes() {
    return this.retentionInMinutes;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ChangeFeedRetentionTimeSpan
});

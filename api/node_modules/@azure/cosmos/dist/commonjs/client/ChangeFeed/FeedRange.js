var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var FeedRange_exports = {};
__export(FeedRange_exports, {
  FeedRange: () => FeedRange,
  FeedRangeInternal: () => FeedRangeInternal
});
module.exports = __toCommonJS(FeedRange_exports);
var import_request = require("../../request/index.js");
class FeedRange {
  /**
   * Min value for the feed range.
   */
  minInclusive;
  /**
   * Max value for the feed range.
   */
  maxExclusive;
  /**
   * @internal
   */
  constructor(minInclusive, maxExclusive) {
    if (new.target === FeedRange) {
      throw new import_request.ErrorResponse("Cannot instantiate abstract class FeedRange");
    }
    this.minInclusive = minInclusive;
    this.maxExclusive = maxExclusive;
  }
}
class FeedRangeInternal extends FeedRange {
  /* eslint-disable @typescript-eslint/no-useless-constructor */
  constructor(minInclusive, maxExclusive) {
    super(minInclusive, maxExclusive);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  FeedRange,
  FeedRangeInternal
});

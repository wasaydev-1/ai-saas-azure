var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ContinuationTokenForPartitionKey_exports = {};
__export(ContinuationTokenForPartitionKey_exports, {
  ContinuationTokenForPartitionKey: () => ContinuationTokenForPartitionKey
});
module.exports = __toCommonJS(ContinuationTokenForPartitionKey_exports);
class ContinuationTokenForPartitionKey {
  /**
   * rid of the container for which the continuation token is issued.
   */
  rid;
  /**
   * A specific Partition key in the container for which the continuation token is issued.
   */
  partitionKey;
  /**
   * Continuation value for the specific Partition key in the container.
   */
  Continuation;
  constructor(rid, partitionKey, continuation) {
    this.rid = rid;
    this.partitionKey = partitionKey;
    this.Continuation = continuation;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ContinuationTokenForPartitionKey
});

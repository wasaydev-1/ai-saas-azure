var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var KeyEncryptionKeyCache_exports = {};
__export(KeyEncryptionKeyCache_exports, {
  KeyEncryptionKeyCache: () => KeyEncryptionKeyCache
});
module.exports = __toCommonJS(KeyEncryptionKeyCache_exports);
var import_KeyEncryptionKey = require("../KeyEncryptionKey.js");
class KeyEncryptionKeyCache {
  // key is JSON.stringify([name, path])
  cache;
  constructor() {
    this.cache = /* @__PURE__ */ new Map();
  }
  getOrCreate(name, path, keyStoreProvider) {
    const key = JSON.stringify([name, path]);
    let keyEncryptionKey = this.get(key);
    if (!keyEncryptionKey) {
      keyEncryptionKey = new import_KeyEncryptionKey.KeyEncryptionKey(name, path, keyStoreProvider);
      this.set(key, keyEncryptionKey);
    }
    return keyEncryptionKey;
  }
  get(key) {
    return this.cache.get(key);
  }
  set(key, keyEncryptionKey) {
    this.cache.set(key, keyEncryptionKey);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  KeyEncryptionKeyCache
});
